const assert = require('assert');

const jsonRpcServer = require('../index.js');
describe("jsonRpcServer", function() {

	it("JsonRpcAnswer", function() {
		var rerver = new jsonRpcServer.JsonRpcAnswer();
		jsonRpcServer.browser_sync_middleware();
	});

});